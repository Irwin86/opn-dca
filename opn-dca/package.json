{
  "name": "opn-dca",
  "version": "1.0.0",
  "description": "Automated DCA Vault on OPN Chain — permissionless, non-custodial, fully on-chain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network opn_testnet",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^3.0.0",
    "@nomicfoundation/hardhat-ethers": "^4.0.12",
    "@nomicfoundation/hardhat-network-helpers": "^3.0.9",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.6",
    "chai": "^6.2.2",
    "ethers": "^6.16.0",
    "hardhat": "^3.7.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^17.4.2"
  },
  "type": "module"
}
